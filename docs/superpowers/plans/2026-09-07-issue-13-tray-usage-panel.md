# Issue #13: existing window as the Codex tray usage panel

## Confirmed scope

The user explicitly confirmed this scope after one clarification question in the fresh Eloop. Convert the sole existing `MainWindow` into a fixed, nonmaximized 640x640 Codex usage panel. A left tray click positions and opens that same window above the actual click point, clamped to the clicked monitor's work area. Decode the `NOTIFYICON_VERSION_4` callback coordinates correctly, including negative screen coordinates; do not substitute a later cursor sample for that click.

Reuse the existing buckets, quota rings, window/reset labels, usage and error states, manual refresh, visibility-controlled automatic refresh, and last complete snapshot. Keep all content reachable by scrolling. Closing or losing focus hides the panel; opening again reuses its window, Codex client and process. `Sair` remains the actual cleanup/exit path. Preserve normal activation, redirected activation, native startup behavior and the existing tray menu.

Embed the user-supplied `gpt-icon-black.png` byte-for-byte as decorative content at the ring center. Do not recolor, redraw, crop, convert or rewrite the source asset. Preserve readable usage, name, window, reset and state text and their accessible equivalents. An image loading failure shows accessible text `Codex`. `chatgpt-icon.webp` is unused. The implementer must locate the supplied attachment and record its source path and SHA-256 before copying it into the package; it is not present in the initial tracked checkout.

No second window, process, timer, data source, provider, dependency, tray library, generic popover abstraction, animation or broad redesign. Prefer small changes to `App.xaml.cs`, `TrayIcon.cs`, `MainWindow.xaml`/`.cs`, `QuotaRing.xaml`/`.cs`, package asset inclusion only if needed, and the existing Core check runner. Keep Core data acquisition/parsing semantics intact. Do not introduce production switches or environment/file markers merely to exercise image failure.

## Loop state

- Phase: implementation
- Clarification questions used: 1
- Round: 1 implementation complete; independent review pending
- Implementation verdict: pending
- Blocker and next action: No implementation blocker. Independent review must verify the scoped diff and then run the unavailable packaged/manual matrix; the implementation agent cannot claim those UI/device cases from local checks.
- Baseline and evidence: Refreshed branch `feature/issue-13-tray-usage-panel`, base HEAD `9290f57737419e901acf909566db02171f4c3dbb`; the pre-existing task spec remains the only unrelated untracked path. Round 1 adds the existing `MainWindow` panel sizing/activation flow, v4 callback point decoding and placement math, the PNG asset, and deterministic Core asserts. Source/repository/unpacked-package PNG SHA-256 is `1D90E8CC56D5285F9144A60F0F4D8ADB7A5C36E7797B26F4E4B34242A0BBD1CC` (source `C:\Users\mateu\.t3\userdata\attachments\63754c50-09c9-486a-bda3-291c4c48b3c9-f2724910-6853-4968-92dd-28d5055c6bc5.png`, repository `src/MyAiUsage.App/Assets/gpt-icon-black.png`, 35,246 bytes). Current code snapshot hashes: `App.xaml.cs` `13433E88CFBD62DB6E439BDB1D4F1F3A41789E17CEDC34AD6B6DA38012B2EDAB`; `TrayIcon.cs` `30C8C9E1151DDB1CBC7506D35D2A5C17416F24533B22A0DC6F1E2E27D53A79BA`; `MainWindow.xaml` `AC8B378030C9D5F8C6E0618B8C2A6B03C1459BE0125FE474DC1F44D49276DA8F`; `MainWindow.xaml.cs` `858525264890F5A5D68CC40604EEE926B7A96B3977B198E20AA2168925C6CA37`; `QuotaRing.xaml` `7A1036B34C5B04089950FD2D8F15C9CA281E6B1FCCF0CDE02175843EFE24203B`; `QuotaRing.xaml.cs` `C7DAD3CE3142E99941E964B58BB571547F14F9D5FD33EA8954464F84CBB34755`; `checks/Program.cs` `A2C2E81420CA1B0C16E4652FBBDF946B66C96A8B4193B0308B270C5DB8E9D50E`.

## Acceptance criteria

- [ ] Left-clicking the tray opens/activates the existing `MainWindow` at fixed 640x640, without maximizing or allowing resize/maximize to defeat the panel size. Repeated openings restore the same window handle and app/client process; no new timer or data source appears.
- [ ] The tray callback passes the actual version-4 click point through to positioning, decoding signed X/Y correctly. The target monitor is selected from that point, and the panel is placed above it then clamped to that monitor's work area. Bottom/top/left/right taskbar and tray overflow clicks do not open on an unrelated monitor or beyond usable bounds when the work area can fit the panel. If a work area cannot fit 640x640, retain the fixed size, pin safely to its work-area origin on the constrained axis, and record the unavoidable fit limitation rather than generating invalid clamp bounds.
- [ ] Positioning uses consistent screen-coordinate units across monitor selection, work area, size and move; changing monitor/DPI does not cause an extra DPI conversion or offset. The implementation records the native sizing units used for 640x640 and validates mixed-DPI behavior.
- [ ] Every existing bucket and window remains individually represented and reachable through scrolling at the fixed size. Percentage, unknown usage, quota threshold/state, window duration and local reset time remain readable; no aggregation, clipping that prevents access, or lost content is introduced.
- [ ] Manual refresh and the existing 60-second visibility-controlled refresh continue using the same client/gate. Hiding stops the existing timer and reopening resumes its existing freshness logic. Loading, available, missing Codex, disconnected, temporary failure, cancelled, partial and stale states retain current behavior; failed/partial reads preserve the last complete snapshot and its timestamp.
- [ ] Close and loss of window activation hide without disposal or process exit; moving keyboard focus between controls inside the panel does not hide it. Reopening after either action works repeatedly, including opening from tray overflow. Normal/redirection activation still restores the same panel; native startup still begins hidden.
- [ ] Tray `Abrir` remains usable. `Sair` disposes the existing client, stops/cancels refresh, removes the tray icon and terminates the app and its owned app-server process tree without an orphan. Hiding must not intercept actual exit.
- [ ] The packaged ring center displays the supplied PNG unchanged, without obscuring or removing readable usage information. The source, repository copy and unpacked package copy have identical SHA-256 hashes; `chatgpt-icon.webp` is neither used nor newly packaged for this task.
- [ ] The logo is decorative, not a duplicate focus target or spoken replacement for quota information. Name, usage, window, reset and state remain available to accessibility tools; image load failure visibly and accessibly falls back to `Codex`. Keyboard refresh, scrolling and existing startup controls remain usable, including in the fallback state.
- [ ] The diff contains only the confirmed panel, asset, relevant checks and task evidence; it preserves unrelated work and introduces none of the excluded architecture/dependencies or production diagnostic hooks.

## Verification

All items start unexecuted. For each completed item record the command/procedure, actual exit code or observed result, evidence artifact path and code snapshot (base SHA plus scoped diff/hash). A build or synthetic callback is not evidence of a real packaged tray interaction. Keep manual cases pending when unavailable; record exact environment limitations and next action instead of marking them passed.

- [x] Run `rtk dotnet restore MyAiUsage.sln`, then `rtk dotnet build MyAiUsage.sln -c Release -p:Platform=x64 -warnaserror`. Actual: restore exit 0; build exit 0 with 4 projects, 0 warnings, 0 errors on base `9290f57737419e901acf909566db02171f4c3dbb` plus the scoped round 1 diff.
- [x] Run `rtk proxy powershell -NoProfile -Command "& '.\checks\MyAiUsage.Core.Checks\bin\x64\Release\net8.0-windows10.0.22000.0\MyAiUsage.Core.Checks.exe'"`. Actual exit 0; output `Tray callback check passed.` and `Core checks passed.`. The existing runner now covers signed positive/negative callback coordinates, the v4 icon-id/event packing, bottom/top/left/right clamps, a negative-origin monitor and undersized work-area axes.
- [x] Run `rtk git diff --check` (exit 0) and inspect scoped diff plus `rtk git status --short`; include the newly added spec/asset in the ownership review and verify unrelated work is preserved. The code and asset hashes are recorded in Loop state above.
- [x] Build the development MSIX from that same snapshot using the established packaging parameters. `rtk proxy msbuild ...` exit 1 because `msbuild` is not on PATH; equivalent `rtk dotnet msbuild MyAiUsage.sln /p:Configuration=Release /p:Platform=x64 /p:UapAppxPackageBuildMode=SideloadOnly /p:AppxBundle=Never /p:AppxPackageDir=artifacts\msix\ /p:GenerateAppxPackageOnBuild=true` exit 0 (one non-fatal missing `mspdbcmf.exe` symbols warning). Package: `src/MyAiUsage.App/artifacts/msix/MyAiUsage.App_0.1.0.0_x64_Test/MyAiUsage.App_0.1.0.0_x64.msix`, identity/version `MyAiUsage/0.1.0.0/x64`, package SHA-256 `B1C963C0589F8F549E0A15C88CA1CE09B5728298ACE6E968890D78FB21EC0C1A`. The unpacked `Assets/gpt-icon-black.png` hash and length match the source/repository (`1D90...BD1CC`, 35,246 bytes); the archive contains zero `chatgpt-icon.webp` entries. Installation/launch and manual tray/UI matrix remain pending.
- [ ] In the packaged app, left-click the actual tray icon on bottom, top, left and right taskbar configurations and from overflow. Record click point, monitor work area, actual panel bounds/size and visible result. Verify above-click placement with necessary edge clamping and repeat open/hide cycles; synthetic messages alone cannot complete this item. If the OS cannot expose an edge configuration, record it as unverified.
- [ ] In the packaged app, test multiple monitors including negative desktop coordinates and mixed DPI (100%, 125%, 150% where available). Verify correct target monitor, fixed size, clamping, no coordinate jumps, and usable scrolling. Record monitor topology/scales and screenshots or observed bounds. Missing hardware is an explicit pending verification limit.
- [ ] In the packaged panel, inspect multiple buckets/windows and unknown/partial/error/stale content; confirm every ring and reset/state label remains reachable. Exercise manual refresh, automatic refresh while visible, hide/reopen freshness behavior and retained snapshot on failure. Use existing testable data/state paths; do not add a second live data source or production failure switch. Record which states are rendered versus only covered by Core checks.
- [ ] In the packaged panel, inspect logo rendering in light/dark/high-contrast settings and keyboard/accessibility output. Verify decorative image semantics, readable usage/state text, refresh and scroll access. Exercise the image-failure path with test-only instrumentation or a separately identified temporary test artifact; show accessible `Codex` and record that artifact separately. Do not modify the supplied PNG or leave test hooks in production. Revalidate the final unmodified package's normal image path.
- [ ] Record the app PID/window handle and owned app-server process IDs before and after close, focus loss, tray reopening, `Abrir` and redirected activation. Internal focus navigation must remain visible; external activation hides. Confirm the same resident instance/client survives hiding, startup behavior remains hidden, and `Sair` leaves no app/tray/owned child process. Record observed cleanup, not just source inspection.

## Remediation

### Round 1 review

## Delivery

- Review verdict and evidence: pending; independent review has not started.
- Delivery status: pending; no issue #13 commit, push or MR created in specification phase.
- MR URL: not created.

Delivery occurs only after the independent implementation verdict is PASS and delivery readiness is rechecked. MR creation/status is not an implementation acceptance criterion. Preserve review history and record the delivered snapshot and actual MR URL; never merge as part of this loop.
